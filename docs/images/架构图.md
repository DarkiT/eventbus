# EventBus 系统架构图

## 整体架构图

```mermaid
graph TB
    subgraph "客户端层"
        Client1["HTTP Handler"]
        Client2["Cron Job"]
        Client3["External API"]
    end
    
    subgraph "API层"
        GlobalAPI["全局单例 API"]
        InstanceAPI["EventBus 实例 API"]
        PipeAPI["泛型管道 API"]
    end
    
    subgraph "核心层 - EventBus 模块拆分"
        EB["eventbus.go 骨架"]
        Admin["admin 管理接口"]
        Batch["batch 批量操作"]
        Channel["channel 通道管理"]
        Helpers["helpers 辅助函数"]
        Match["match 匹配聚合"]
        Publish["publish 发布核心"]
        Response["response 响应收敛"]
        ResponseAPI["response_api 对外API"]
        Subscribe["subscribe 订阅管理"]
        Pipe["pipe 泛型管道"]
        Singleton["singleton 全局单例"]
    end
    
    subgraph "扩展层 - v0.3.0 新增"
        Retry["retry 重试机制"]
        Trie["topic_trie 匹配树"]
        Adapter["adapter 适配器"]
    end
    
    subgraph "处理层"
        FilterChain["过滤器链"]
        MiddlewareChain["中间件链"]
        HandlerMgr["处理器管理"]
    end
    
    subgraph "生命周期层"
        Shutdown["Shutdown 优雅关闭"]
        Close["Close 立即关闭"]
    end
    
    subgraph "存储层"
        COWMap["COW 映射"]
        ChannelPool["通道池"]
        Registry["处理器注册表"]
    end
    
    subgraph "监控层"
        Tracer["事件追踪器"]
        Metrics["性能指标"]
        HealthCheck["健康检查"]
    end
    
    Client1 & Client2 & Client3 --> GlobalAPI & InstanceAPI & PipeAPI
    
    GlobalAPI --> Singleton
    InstanceAPI --> EB
    PipeAPI --> Pipe
    
    EB --> Admin & Batch & Channel & Helpers & Match & Publish & Response & ResponseAPI & Subscribe
    
    EB --> Retry
    Channel --> Trie
    Pipe --> Adapter
    
    EB --> FilterChain & MiddlewareChain
    Admin --> HandlerMgr
    
    FilterChain & MiddlewareChain --> COWMap
    HandlerMgr --> ChannelPool & Registry
    ChannelPool --> COWMap
    
    EB --> Shutdown & Close
    
    EB --> Tracer
    Pipe --> Tracer
    Tracer --> Metrics
    EB --> HealthCheck
    
    classDef client fill:#e1f5fe
    classDef api fill:#f3e5f5
    classDef core fill:#e8f5e8
    classDef ext fill:#fff8e1
    classDef proc fill:#fff3e0
    classDef life fill:#fce4ec
    classDef store fill:#e0f2f1
    classDef mon fill:#f1f8e9
    
    class Client1,Client2,Client3 client
    class GlobalAPI,InstanceAPI,PipeAPI api
    class EB,Admin,Batch,Channel,Helpers,Match,Publish,Response,ResponseAPI,Subscribe,Pipe,Singleton core
    class Retry,Trie,Adapter ext
    class FilterChain,MiddlewareChain,HandlerMgr proc
    class Shutdown,Close life
    class COWMap,ChannelPool,Registry store
    class Tracer,Metrics,HealthCheck mon
```

## 核心组件架构

### EventBus 核心架构

```mermaid
graph TB
    subgraph "EventBus 骨架"
        EB["eventbus.go"]
        Config["配置管理"]
        State["状态管理 (atomic)"]
    end
    
    subgraph "订阅管理"
        Sub["eventbus_subscribe.go"]
        SubOnce["SubscribeOnce 一次性订阅"]
        SubPriority["优先级订阅"]
        SubFilter["过滤器订阅"]
        SubReliable["SubscribeReliable 可靠订阅"]
    end
    
    subgraph "发布管理"
        Pub["eventbus_publish.go"]
        PubSync["PublishSync 同步"]
        PubAsync["Publish 异步"]
        PubCtx["PublishWithContext"]
        PubBatch["eventbus_batch.go"]
        PubMatch["eventbus_match.go"]
    end
    
    subgraph "响应式发布"
        Resp["eventbus_response.go"]
        RespAll["PublishSyncAll"]
        RespAny["PublishSyncAny"]
        RespValue["PublishSyncAnyValue"]
        RespAPI["eventbus_response_api.go"]
    end
    
    subgraph "通道与队列"
        Ch["eventbus_channel.go"]
        ChWorker["Worker 协程"]
        ChQueue["消息队列"]
        ChDeliver["投递与退订"]
    end
    
    subgraph "管理接口"
        Adm["eventbus_admin.go"]
        AdmClose["Close/Shutdown"]
        AdmStats["统计信息"]
        AdmHealth["健康检查"]
    end
    
    subgraph "辅助模块"
        Hlp["eventbus_helpers.go"]
        Err["errors.go"]
        Iface["interfaces.go"]
    end
    
    EB --> Config & State
    EB --> Sub & Pub & Resp & Ch & Adm & Hlp
    
    Sub --> SubOnce & SubPriority & SubFilter & SubReliable
    Pub --> PubSync & PubAsync & PubCtx
    Pub --> PubBatch & PubMatch
    Resp --> RespAll & RespAny & RespValue & RespAPI
    Ch --> ChWorker & ChQueue & ChDeliver
    Adm --> AdmClose & AdmStats & AdmHealth
    
    SubReliable --> Retry["retry.go 重试"]
    PubMatch --> Trie["topic_trie.go 匹配"]
    
    classDef skeleton fill:#e8f5e8
    classDef sub fill:#e1f5fe
    classDef pub fill:#fff3e0
    classDef resp fill:#f3e5f5
    classDef channel fill:#fce4ec
    classDef admin fill:#e0f2f1
    classDef helper fill:#f5f5f5
    
    class EB,Config,State skeleton
    class Sub,SubOnce,SubPriority,SubFilter,SubReliable sub
    class Pub,PubSync,PubAsync,PubCtx,PubBatch,PubMatch pub
    class Resp,RespAll,RespAny,RespValue,RespAPI resp
    class Ch,ChWorker,ChQueue,ChDeliver channel
    class Adm,AdmClose,AdmStats,AdmHealth admin
    class Hlp,Err,Iface,Retry,Trie helper
```

### 泛型管道架构

```mermaid
graph TB
    subgraph "泛型管道"
        Pipe["Pipe[T]"]
        TypeSafety["类型安全"]
        BufferMgr["缓冲管理"]
    end
    
    subgraph "处理器管理"
        TypedHandlers["类型化处理器"]
        PriorityMgr["优先级管理"]
        HandlerExec["处理器执行器"]
    end
    
    subgraph "消息传递"
        TypedChannel["类型化通道"]
        MsgBuffer["消息缓冲"]
        AsyncProcessor["异步处理器"]
    end
    
    subgraph "生命周期"
        ContextMgr["上下文管理"]
        StateMgr["状态管理"]
        ResourceMgr["资源管理"]
    end
    
    Pipe --> TypeSafety
    Pipe --> BufferMgr
    Pipe --> TypedHandlers
    Pipe --> ContextMgr
    
    TypedHandlers --> PriorityMgr
    TypedHandlers --> HandlerExec
    
    BufferMgr --> TypedChannel
    TypedChannel --> MsgBuffer
    MsgBuffer --> AsyncProcessor
    
    AsyncProcessor --> HandlerExec
    
    ContextMgr --> StateMgr
    StateMgr --> ResourceMgr
    
    classDef pipeComp fill:#e8f5e8
    classDef handlerComp fill:#fff3e0
    classDef msgComp fill:#fce4ec
    classDef lifecycleComp fill:#f1f8e9
    
    class Pipe,TypeSafety,BufferMgr pipeComp
    class TypedHandlers,PriorityMgr,HandlerExec handlerComp
    class TypedChannel,MsgBuffer,AsyncProcessor msgComp
    class ContextMgr,StateMgr,ResourceMgr lifecycleComp
```

## Adapter 适配器架构

### 三层抽象模型

```mermaid
graph TB
    subgraph "应用层"
        App["业务代码"]
        EB["EventBus 实例"]
    end
    
    subgraph "Adapter 适配器层"
        Bridge["Bridge 桥接器"]
        Transport["Transport 传输层"]
        Codec["Codec 编解码器"]
    end
    
    subgraph "消息处理"
        Message["Message 消息模型"]
        Context["Context 请求上下文"]
        Dedupe["Dedupe 去重机制"]
    end
    
    subgraph "外部系统"
        MQTT["MQTT Broker"]
        Kafka["Apache Kafka"]
        Redis["Redis Pub/Sub"]
        HTTP["HTTP Webhook"]
    end
    
    subgraph "配置"
        Options["Options 可配置选项"]
    end
    
    App --> EB
    EB --> Bridge
    Bridge --> Transport
    Bridge --> Codec
    Bridge --> Message
    Bridge --> Context
    Bridge --> Dedupe
    Bridge --> Options
    
    Transport --> MQTT
    Transport --> Kafka
    Transport --> Redis
    Transport --> HTTP
    
    Codec --> Message
    
    classDef app fill:#e8f5e8
    classDef adapter fill:#fff3e0
    classDef msg fill:#e1f5fe
    classDef ext fill:#fce4ec
    classDef cfg fill:#f3e5f5
    
    class App,EB app
    class Bridge,Transport,Codec adapter
    class Message,Context,Dedupe msg
    class MQTT,Kafka,Redis,HTTP ext
    class Options cfg
```

### 消息桥接流程

```mermaid
graph LR
    Publish["Publish 发布事件"] --> Bridge["Bridge.Send()"]
    Bridge --> Encode["Codec.Encode 编码"]
    Encode --> Transport["Transport.Send 传输"]
    Transport --> External["外部系统"]
    
    External --> Recv["Transport.Receive"]
    Recv --> Decode["Codec.Decode 解码"]
    Decode --> Dedupe["Dedupe 去重检查"]
    Dedupe -->|新消息| EB["EventBus 重新发布"]
    Dedupe -->|重复| Drop["静默丢弃"]
    
    classDef flow fill:#2196F3,color:#fff
    class Publish,Bridge,Encode,Transport,External,Recv,Decode,Dedupe,EB flow
    class Drop fill:#F44336,color:#fff
```

## 数据流架构

### 事件发布数据流

```mermaid
flowchart TD
    Start([开始发布]) --> ValidateInput{验证输入}
    ValidateInput -->|无效| ReturnError([返回错误])
    ValidateInput -->|有效| ApplyFilters[应用过滤器]
    
    ApplyFilters --> FilterCheck{过滤检查}
    FilterCheck -->|拒绝| SilentDrop([静默丢弃])
    FilterCheck -->|通过| ApplyMiddleware[应用中间件]
    
    ApplyMiddleware --> BeforeProcess[前置处理]
    BeforeProcess --> TopicMatch[主题匹配]
    
    TopicMatch --> FindChannels[查找匹配通道]
    FindChannels --> HasChannels{存在通道?}
    HasChannels -->|否| NoSubscriber([无订阅者])
    HasChannels -->|是| DispatchMsg[分发消息]
    
    DispatchMsg --> AsyncCheck{异步发布?}
    AsyncCheck -->|是| AsyncDispatch[异步分发]
    AsyncCheck -->|否| SyncDispatch[同步分发]
    
    AsyncDispatch --> QueueMsg[消息入队]
    QueueMsg --> QueueFull{队列满?}
    QueueFull -->|是| QueueFullEvent[队列满事件]
    QueueFull -->|否| AsyncReturn([异步返回])
    
    SyncDispatch --> DirectExec[直接执行]
    DirectExec --> HandlerExec[处理器执行]
    HandlerExec --> SyncReturn([同步返回])
    
    QueueFullEvent --> AsyncReturn
    
    AsyncReturn --> AfterProcess[后置处理]
    SyncReturn --> AfterProcess
    AfterProcess --> Complete([完成])
    
    classDef startEnd fill:#e8f5e8
    classDef process fill:#e1f5fe
    classDef decision fill:#fff3e0
    classDef error fill:#ffebee
    
    class Start,Complete,AsyncReturn,SyncReturn startEnd
    class ApplyFilters,ApplyMiddleware,BeforeProcess,TopicMatch,FindChannels,DispatchMsg,AsyncDispatch,SyncDispatch,QueueMsg,DirectExec,HandlerExec,AfterProcess process
    class ValidateInput,FilterCheck,HasChannels,AsyncCheck,QueueFull decision
    class ReturnError,SilentDrop,NoSubscriber,QueueFullEvent error
```

### 订阅管理数据流

```mermaid
flowchart TD
    Subscribe([订阅请求]) --> ValidateHandler{验证处理器}
    ValidateHandler -->|无效| HandlerError([处理器错误])
    ValidateHandler -->|有效| NormalizeTopic[标准化主题]
    
    NormalizeTopic --> FindChannel[查找通道]
    FindChannel --> ChannelExists{通道存在?}
    ChannelExists -->|否| CreateChannel[创建通道]
    ChannelExists -->|是| UseExisting[使用现有通道]
    
    CreateChannel --> InitChannel[初始化通道]
    InitChannel --> StartLoop[启动消息循环]
    StartLoop --> StoreChannel[存储通道]
    StoreChannel --> AddHandler[添加处理器]
    
    UseExisting --> AddHandler
    AddHandler --> CreateHandlerInfo[创建处理器信息]
    CreateHandlerInfo --> SortByPriority[按优先级排序]
    SortByPriority --> UpdateRegistry[更新注册表]
    UpdateRegistry --> NotifyTracer[通知追踪器]
    NotifyTracer --> SubscribeComplete([订阅完成])
    
    Unsubscribe([取消订阅]) --> FindHandler[查找处理器]
    FindHandler --> HandlerFound{找到处理器?}
    HandlerFound -->|否| NotFoundError([未找到错误])
    HandlerFound -->|是| RemoveHandler[移除处理器]
    RemoveHandler --> UpdateList[更新处理器列表]
    UpdateList --> CleanupCheck{需要清理?}
    CleanupCheck -->|是| CleanupChannel[清理通道]
    CleanupCheck -->|否| UnsubscribeComplete([取消订阅完成])
    CleanupChannel --> UnsubscribeComplete
    
    classDef startEnd fill:#e8f5e8
    classDef process fill:#e1f5fe
    classDef decision fill:#fff3e0
    classDef error fill:#ffebee
    
    class Subscribe,SubscribeComplete,Unsubscribe,UnsubscribeComplete startEnd
    class NormalizeTopic,FindChannel,CreateChannel,InitChannel,StartLoop,StoreChannel,UseExisting,AddHandler,CreateHandlerInfo,SortByPriority,UpdateRegistry,NotifyTracer,FindHandler,RemoveHandler,UpdateList,CleanupChannel process
    class ValidateHandler,ChannelExists,HandlerFound,CleanupCheck decision
    class HandlerError,NotFoundError error
```

## 并发架构

### 并发控制架构

```mermaid
graph TB
    subgraph "读写控制"
        RWMutex["读写互斥锁"]
        AtomicBool["原子布尔值"]
        AtomicCounter["原子计数器"]
    end
    
    subgraph "COW机制"
        COWMap["COW映射"]
        ReadSnapshot["读快照"]
        WriteSnapshot["写快照"]
    end
    
    subgraph "通道并发"
        ChannelPool["通道池"]
        MsgQueue["消息队列"]
        WorkerPool["工作池"]
    end
    
    subgraph "上下文管理"
        ContextTree["上下文树"]
        CancelSignal["取消信号"]
        TimeoutMgr["超时管理"]
    end
    
    subgraph "资源管理"
        ResourcePool["资源池"]
        GCManager["垃圾回收管理"]
        MemoryPool["内存池"]
    end
    
    RWMutex --> COWMap
    AtomicBool --> ChannelPool
    AtomicCounter --> WorkerPool
    
    COWMap --> ReadSnapshot
    COWMap --> WriteSnapshot
    
    ChannelPool --> MsgQueue
    MsgQueue --> WorkerPool
    
    ContextTree --> CancelSignal
    CancelSignal --> TimeoutMgr
    
    WorkerPool --> ResourcePool
    ResourcePool --> GCManager
    GCManager --> MemoryPool
    
    classDef lockComp fill:#e8f5e8
    classDef cowComp fill:#e1f5fe
    classDef channelComp fill:#fff3e0
    classDef contextComp fill:#fce4ec
    classDef resourceComp fill:#f1f8e9
    
    class RWMutex,AtomicBool,AtomicCounter lockComp
    class COWMap,ReadSnapshot,WriteSnapshot cowComp
    class ChannelPool,MsgQueue,WorkerPool channelComp
    class ContextTree,CancelSignal,TimeoutMgr contextComp
    class ResourcePool,GCManager,MemoryPool resourceComp
```

## EventBus 关闭与优雅关闭

```mermaid
flowchart TD
    A[触发关闭] --> B{关闭类型}
    B -->|Close 立即关闭| C[设置关闭状态标志]
    B -->|Shutdown 优雅关闭| D[设置关闭状态标志]
    
    C --> E[立即停止接收新发布]
    D --> F[拒绝新发布但等待进行中完成]
    
    E --> G[遍历所有通道]
    F --> H[排空进行中的消息]
    H --> G
    
    G --> I[关闭单个通道]
    I --> J[取消上下文/停止循环]
    J --> K[清理处理器和队列]
    
    K --> L{还有其他通道?}
    L -->|是| G
    L -->|否| M[清理全局资源]
    
    M --> N[通知追踪器关闭]
    N --> O[关闭完成]
    
    classDef start fill:#4CAF50,color:#fff
    classDef close fill:#2196F3,color:#fff
    classDef shutdown fill:#FF9800,color:#fff
    classDef proc fill:#9C27B0,color:#fff
    
    class A,O start
    class C,E close
    class D,F,H shutdown
    class G,I,J,K,L,M,N proc
```

## 扩展架构

### 插件系统架构

```mermaid
graph TB
    subgraph "插件接口"
        FilterInterface["过滤器接口"]
        MiddlewareInterface["中间件接口"]
        TracerInterface["追踪器接口"]
    end
    
    subgraph "插件管理"
        PluginRegistry["插件注册表"]
        PluginLoader["插件加载器"]
        PluginLifecycle["插件生命周期"]
    end
    
    subgraph "内置插件"
        LogFilter["日志过滤器"]
        MetricsMiddleware["指标中间件"]
        DebugTracer["调试追踪器"]
    end
    
    subgraph "自定义插件"
        CustomFilter["自定义过滤器"]
        CustomMiddleware["自定义中间件"]
        CustomTracer["自定义追踪器"]
    end
    
    subgraph "插件配置"
        ConfigManager["配置管理器"]
        HotReload["热重载"]
        VersionControl["版本控制"]
    end
    
    FilterInterface --> PluginRegistry
    MiddlewareInterface --> PluginRegistry
    TracerInterface --> PluginRegistry
    
    PluginRegistry --> PluginLoader
    PluginLoader --> PluginLifecycle
    
    PluginLifecycle --> LogFilter
    PluginLifecycle --> MetricsMiddleware
    PluginLifecycle --> DebugTracer
    
    PluginLifecycle --> CustomFilter
    PluginLifecycle --> CustomMiddleware
    PluginLifecycle --> CustomTracer
    
    PluginLoader --> ConfigManager
    ConfigManager --> HotReload
    HotReload --> VersionControl
    
    classDef interfaceComp fill:#e8f5e8
    classDef mgmtComp fill:#e1f5fe
    classDef builtinComp fill:#fff3e0
    classDef customComp fill:#fce4ec
    classDef configComp fill:#f1f8e9
    
    class FilterInterface,MiddlewareInterface,TracerInterface interfaceComp
    class PluginRegistry,PluginLoader,PluginLifecycle mgmtComp
    class LogFilter,MetricsMiddleware,DebugTracer builtinComp
    class CustomFilter,CustomMiddleware,CustomTracer customComp
    class ConfigManager,HotReload,VersionControl configComp
```

### 分布式扩展架构（未来规划）

```mermaid
graph TB
    subgraph "本地节点"
        LocalEventBus["本地事件总线"]
        LocalHandlers["本地处理器"]
        LocalCache["本地缓存"]
    end
    
    subgraph "集群管理"
        NodeDiscovery["节点发现"]
        LoadBalancer["负载均衡器"]
        FailoverMgr["故障转移管理"]
    end
    
    subgraph "消息传输"
        MessageBroker["消息代理"]
        Serializer["序列化器"]
        NetworkLayer["网络层"]
    end
    
    subgraph "一致性保证"
        ConsensusAlgo["共识算法"]
        ReplicationMgr["复制管理器"]
        ConflictResolver["冲突解决器"]
    end
    
    subgraph "监控告警"
        ClusterMonitor["集群监控"]
        HealthChecker["健康检查器"]
        AlertManager["告警管理器"]
    end
    
    LocalEventBus --> NodeDiscovery
    LocalHandlers --> LoadBalancer
    LocalCache --> MessageBroker
    
    NodeDiscovery --> LoadBalancer
    LoadBalancer --> FailoverMgr
    
    MessageBroker --> Serializer
    Serializer --> NetworkLayer
    
    NetworkLayer --> ConsensusAlgo
    ConsensusAlgo --> ReplicationMgr
    ReplicationMgr --> ConflictResolver
    
    FailoverMgr --> ClusterMonitor
    ClusterMonitor --> HealthChecker
    HealthChecker --> AlertManager
    
    classDef localComp fill:#e8f5e8
    classDef clusterComp fill:#e1f5fe
    classDef msgComp fill:#fff3e0
    classDef consistencyComp fill:#fce4ec
    classDef monitorComp fill:#f1f8e9
    
    class LocalEventBus,LocalHandlers,LocalCache localComp
    class NodeDiscovery,LoadBalancer,FailoverMgr clusterComp
    class MessageBroker,Serializer,NetworkLayer msgComp
    class ConsensusAlgo,ReplicationMgr,ConflictResolver consistencyComp
    class ClusterMonitor,HealthChecker,AlertManager monitorComp
```